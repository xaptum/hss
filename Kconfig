#
# XAP-RC-00x
#

config  XAPRC00X
        tristate "Xaptum Router Card Proxy Driver"
        depends on USB_SUPPORT
        ---help---

          Say Y here if you want to support the Xaptum Router Card
          proxy drivers (XAP-RC-00x). 

          To compile this driver as a module, choose M here. The
          module will be called xaprcd.

If unsure, say M.